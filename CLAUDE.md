# Repo instructions

- Both people working on this app/repo (the user, and his partner who also runs Claude Code sessions on this same repo) are male. Any Arabic-language text you write — chat replies, TODO.md entries, commit messages, anything addressed to "the user" — must use masculine grammatical forms exclusively (e.g. "شغّله"/"جرّب"/"تأكد"/"خد"), never feminine forms ("شغّليه"/"جربي"/"تأكدي"/"خدي"). This applies in every session on this repo, not just this one.
- After pushing a branch with completed work, automatically open a pull request (no need to ask first). Only skip this if the user explicitly says not to.
- After opening a pull request, automatically merge it too (no need to ask first), so changes reach `main`/Vercel production without an extra round trip. Only skip this if the user explicitly says not to, or the PR has unresolved review comments/failing checks.
- You are connected to this project's Supabase directly (MCP tools). Whenever you create/update a `supabase/phaseNN_*.sql` (or any other SQL) file, apply it yourself right away (`apply_migration`) — don't hand it to the user to run themselves, and don't ask first. Standing instruction as of 2026-08-20; only skip this if the user explicitly says not to for that specific change. After applying it, run `supabase/schema_health_check.sql` yourself too (`execute_sql`) and confirm every row says "OK" before telling the user it's done — this exists because of a real incident (2026-07-24, see `TODO.md`): a migration silently never fully applied, and a later unrelated change assumed it had, causing the entire patient list to vanish with no visible error. If a migration is genuinely destructive/high-risk (e.g. drops real data, not just adds a column/function) or the user hasn't set up this session's Supabase MCP connection, fall back to handing the file over with the same schema_health_check.sql reminder as before.
- Never run the full Playwright suite (`npx playwright test` with no file filter) unless the user explicitly asks for it first. Standing instruction as of 2026-08-20 — a targeted subset (the changed area + anything plausibly touched) is the default; only run everything when asked.
- For any real (non-automated-test) migration-import test the user asks for: build the sample export ZIP yourself (e.g. ~10 patients with rich/varied data per patient, covering Diagnose/Verordnungen/Laborbefunde/Befunde etc. so the test is actually thorough) and hand it to him as a file. He uploads it himself through the real live app using his own `Dr.test` doctor account — don't upload it into any real backend yourself.
